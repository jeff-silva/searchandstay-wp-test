# Search and Stay Wordpress Test

Run with Docker
```bash
docker-compose up
```

When activating the plugin, the configuration page will be available in two places:

`Menu > Search and Stay Settings` <br />
`Menu > Settings > Search and Stay Settings`